<? /** @var $block array */ ?><?
/**
 * Блок «Галерея» в наборе для карточки товара.
 *
 * Раньше он рисовал свою разметку (.row.galley > ul.module-gallery-list) и
 * поэтому выглядел иначе, чем на остальных страницах. По макету галерея везде
 * одна: квадратные плитки по пять в ряд (оформление — .sp-gallery в
 * css/newdesign.css). Остальные пять наборов блоков уже печатают .sp-gallery,
 * поэтому здесь просто подключаем общий файл, а не держим шестую копию —
 * $block и $this внутри include остаются те же.
 */
include __DIR__.'/../.default/gallery.php';
