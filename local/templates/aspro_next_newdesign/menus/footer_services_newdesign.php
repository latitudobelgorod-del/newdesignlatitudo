<?
// Колонка подвала «Услуги» (новый дизайн).
// Копия меню `servises_short` старого дизайна: здесь только корневой пункт,
// а сами услуги подтягиваются из инфоблока файлом /services/.left.menu_ext.php
// (он же фильтрует их по региону). Поэтому список правится не тут, а в
// инфоблоке услуг — тут задаётся только раздел, от которого берутся дети.
// Подключается из корневого .footer_services_newdesign.menu.php.
$aMenuLinks = Array(
	Array(
		"Услуги",
		"/services/",
		Array(),
		Array(),
		""
	)
);
?>
