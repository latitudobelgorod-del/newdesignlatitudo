<? if (!defined('B_PROLOG_INCLUDED') || B_PROLOG_INCLUDED !== true) die(); ?>
<?
/**
 * Главная нового дизайна. Сквозной баннер — сразу под блоком «Категории
 * товаров», отдельным широким блоком во всю ширину контейнера.
 *
 * Картинку, ссылку, активность и сроки показа даёт сервис LatitudoBanner
 * (local/php_interface/include/latitudo_banner.php): на сайте всегда ровно один
 * текущий баннер, и запускают новый заменой данных в его записи.
 *
 * Пришёл на место pergola_banner_mainpage.php — того же места и той же ширины
 * блока, только содержимое теперь не включаемая область, которую правили
 * руками в html, а запись инфоблока с датами показа и отдельными картинками
 * для главной и каталога (Ирина, 3 сентября 2026).
 *
 * Класс .nd-mainbanner оставлен прежний: его оформление уже есть в
 * css/newdesign.css и оно ровно то, что нужно — блок по ширине контейнера со
 * скруглением у картинки.
 */

$ndBanner = class_exists('LatitudoBanner') ? LatitudoBanner::homeBanner() : null;

// Показывать нечего — не печатаем даже пустую обёртку, иначе на главной
// останется лишний отступ между категориями и следующим блоком.
if (!$ndBanner) {
    return;
}

echo LatitudoBanner::render($ndBanner, 'nd-mainbanner');
