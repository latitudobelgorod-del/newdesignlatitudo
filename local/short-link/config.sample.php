<?php
/**
 * Настройки API коротких ссылок.
 *
 * Реальный config.php лежит ВНЕ Git (см. .gitignore) — в нём боевой токен.
 * Этот файл — образец: скопировать в config.php и подставить свой токен.
 *
 *     cp config.sample.php config.php
 *
 * Токен можно не хранить в файле вовсе, а отдать окружению: если задана
 * переменная ND_SHORT_LINK_TOKEN, берётся она (в Apache — SetEnv в .htaccess
 * уровнем выше, чтобы сам .htaccess этой папки остался в Git без секретов).
 */

// Токен, который приложение шлёт в заголовке X-Api-Token.
define('ND_SHORT_LINK_TOKEN', getenv('ND_SHORT_LINK_TOKEN') ?: 'ПОДСТАВЬ_СВОЙ_ТОКЕН');

// Домен, от которого собирается короткая ссылка. Региональные поддомены
// сознательно не учитываем — приложению нужен один постоянный адрес.
define('ND_SHORT_LINK_HOST', 'https://latitudo.ru');

// Инфоблок каталога товаров.
define('ND_SHORT_LINK_IBLOCK', 19);

// Свойство, в котором ссылка видна у элемента в админке.
define('ND_SHORT_LINK_PROP', 'ND_SHORT_URI');

// Белый список IP. Пустой массив — проверка отключена (пускаем по токену).
$ND_SHORT_LINK_IPS = [];
